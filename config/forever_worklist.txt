# forever_runner worklist — one experiment per line, top = highest priority.
# The runner runs the first line it hasn't finished yet (tracked in results/logs/forever_worklist.done,
# so this file is NEVER rewritten by the runner — edit it freely while it runs).
# When every line is done, the runner falls back to the low-value ntru_b2 seed
# filler so the box never idles. Inject higher-value work = add a line (or reorder).
#
# FORMAT:  <campaign> [--n N] [--q Q] [--beta B] [--seeds S] [--workers W] [@budget_h=H]
#   - <campaign> must exist in config/sweep.toml.
#   - @budget_h=H  optional wall-clock cap for that line (kills at ~boundary, moves on).
#   - blank lines and # comments ignored.
#   - to RE-run a completed line, remove its entry from results/logs/forever_worklist.done (or tweak the line).
#
# COST-PROBE CONVENTION: to cost an un-costed config, use `--seeds 8 --workers 8 @budget_h=14`,
#   NOT `--seeds 1 --workers 1` with no cap.
#   One fplll seed = one core (beta-enum single-threaded), so a 1-seed probe idles ~23 cores for its whole
#   run. 8 parallel seeds have the SAME wall time but pin 8 cores and return 8 cost samples (MAD/spread)
#   vs 1 — strictly better recon. (2026-08-11 owner: "test while using CPU at higher %".)
#   ALWAYS @budget_h on a probe: an un-costed config can hot-wedge with no self-terminate (INC-54 below).
#
# ---- REAL SCIENCE (drains top-to-bottom before any filler) --------------------
#
# #1 the bumped-beta wall test (B2 science review): can a stronger blocksize CRACK the
#    n=179 beta40 near-miss (ratio 57 @ 2.909x)?
#
#    STATUS 2026-08-27: OFF-BOX, STATE UNVERIFIED SINCE 2026-08-23 -- do NOT
#    repeat "it is running" as fact. Either way this line stays off this runner:
#    the cell is deck-owned. On 2026-08-23 10:18 AEST the steamdeck node
#    (deck@192.168.1.229, rootless podman, 6 physical Zen 4 cores) was launched on
#    seeds 5-10 -- the six that remained of num_seeds=10 -- as lingering user unit
#    ntru-wall-b50-n179.service with `--beta 50 --workers 6 --seed-timeout-s 86400`.
#    Since then INC-58 (detected 2026-08-24) found the node's rootless-podman unit
#    could not write its bind-mount at all without --userns=keep-id, and the node was
#    dead 36 h. Nothing from that run has landed here: as of 2026-08-27
#    results/seeds/ntru_g6k/q4591/p500_mt50/n179_beta50/ still holds only seeds 1-4
#    (all mtime 2026-08-19). That is NOT proof the run died -- node seeds only arrive
#    via `make node-pull`. Run `make node-status`, then `make node-pull`, before
#    relaunching or re-planning this line. Adding a line here would duplicate
#    deck-owned work.
#    2026-08-23 evening note, still current: the steamdeck node runs its own
#    forever-runner (config/forever-runner-steamdeck.service, worklist
#    config/forever_worklist_steamdeck.txt) gated to start when the wall unit exits;
#    n=181 beta50 is queued THERE and the node owns the ntru_g6k beta40 backfill cells
#    167:3167 / 173:4073 / 179:4591 as its idle floor -- never add those cells, or
#    n=181 beta50, to THIS worklist.
#
#    The 2026-08-12 g6k re-route (below) was only half the blocker. The other half:
#    seed_wall_s = 14400 in config/sweep.toml is 0.30x a healthy seed (MEASURED
#    13.13-13.46 h from seeds 1-4) and SIGKILLed seeds 5-20 on 2026-08-20 -- see
#    results/wall_cap_events.jsonl. The owner authorised 86400 for the off-box run,
#    passed on the CLI so seed_wall_s stays at its reserved value here. beta=60 is
#    still blocked on the g6k gauss_sieve assertion and was excluded by pinning
#    --beta 50; beta_grid in sweep.toml still lists it, so any relaunch WITHOUT
#    that pin will hit the assertion.
#
#    NET beta=50 RESULT TO DATE: no crack-rate signal at all. Only 4 seeds have ever
#    completed at this cell (seeds 1-4, 2026-08-19); the 16 wall_cap_kill records from
#    run 30d67c21b03d (cap_s=14400, elapsed 14401.0-14401.9 s) carry ZERO information
#    about beta=50 because the cap was 0.30x a healthy seed -- every kill was by
#    construction. Quote this cell as a lower bound only; do not report a beta=50 rate.
#
#    Original 2026-08-12 diagnosis, kept as the record:
#    DISABLED 2026-08-12 (INC-54): the beta50
#    probe HOT-WEDGED for ~25h inside ONE fplll enumeration call (voluntary_ctxt_switches=1,
#    zero heartbeats, 1 core pinned) and never returned. Root cause = fplll enum ceiling
#    ~dim 300 (memory: n157_fplll_ceiling); n=179 is dim 358, far past it. beta40 works there
#    (shorter enum) but beta50's enum radius blows up. fplll is the WRONG TOOL at dim >=~300 —
#    re-route the wall test to g6k (needs sdbkz-g6k:ci image; memory: g6k_vm_image_gap), do
#    NOT re-enable on fplll. Lines kept commented as a record:
# ntru_wall_beta_bump --n 179 --q 4591 --beta 50 --seeds 8 --workers 8 @budget_h=14   # ONLY via g6k
# ntru_wall_beta_bump --n 179 --q 4591 --workers 10                                    # ONLY via g6k
# ntru_wall_beta_bump --n 181 --q 4703 --workers 10                                    # ONLY via g6k
#    ^ DESIGN cell = 4703 (2.90x q_fat(181)=1622.3), matching n=179's 2.909x. The deck
#      actually queued q=4591 (2.83x) -- config/forever_worklist_steamdeck.txt:32, and the
#      deck header claims the q4591 beta50 cells for BOTH n=179 and n=181. Decide which
#      ratio you want before relaunching; they are different cells.
#
# #2 n=173 matched-ratio wall extension (2026-08-15). WHY: the "frontier dies 167->173"
#    headline is confounded by SAMPLING, not just dimension. n=167 was pushed to 4.97x q_fat
#    (SD DSD-rate wanders 20-45%, never crosses 50% => soft-wall); n=173 was only sampled to
#    3.43x (q4967, SD 0-10%). At MATCHED ratio 173 does run lower (~4% vs 167's ~15%) but that
#    band is noisy (n=20) and 173 was NEVER sampled in 167's hot 3.5-5.0x band. These 5 cells
#    close that gap: does 173 climb to 167's ~30% plateau (=> frontier is a sampling floor, 173
#    crackable at high q, HEADLINE CHANGE) or stay flat ~5% (=> real dim-driven wall, headline
#    STRENGTHENED with matched-ratio data)? Either outcome is a clean frontier-death datapoint.
#    Design = onset_driver's own --push-walls 173 grid (0.30x q_fat step from the q4967 max-null
#    to QCAP 5.0x=7250): q 5407/5843/6287/6733/7177 = 3.73/4.03/4.34/4.64/4.95x, all prime.
#    COST (MEASURED same-regime, NOT estimator — it under-predicts 2.3x here): existing n=173
#    cells ran median 9.9h(q4513)->10.8h(q4967), rising ~1h/450q => ~11-15h/seed at these q;
#    20 seeds/20 workers = 1 wave, so ~11-15h/cell, ~60-66h total. No @budget_h => the 24h
#    DEFAULT_MAX_WALL_H backstop kills a hang without clipping legit work. beta=40 short-enum
#    (dim 346, push173 proved tractable to q4967, no INC-54 wedge). Writes to canonical
#    results/seeds/ntru/ (no seed_tag) so extract_dsd_onset merges with the existing 173 ladder.
ntru_onset_boundary --n 173 --q 5407 --workers 20
ntru_onset_boundary --n 173 --q 5843 --workers 20
ntru_onset_boundary --n 173 --q 6287 --workers 20
ntru_onset_boundary --n 173 --q 6733 --workers 20
ntru_onset_boundary --n 173 --q 7177 --workers 20
#    ^ DONE 2026-08-18: dim-driven wall CONFIRMED (SD 0-10% flat across 167's hot band vs
#      167's 20-45% plateau; no 50% crossing to QCAP 5x). paper_findings 2026-08-18.
#
# #3 n=157 BKZ-onset extension (2026-08-18). WHY: the 50%-DSD-crossing frontier readout over
#    the landed corpus shows the CLEAN onset tiers: 149/151 both oracles cross; 157 SD crosses
#    (q2354 = 2.07x q_fat) but BKZ NEVER — its grid stops at q2411 = 2.12x with BKZ at 15%,
#    i.e. 157 is UNDER-SAMPLED exactly where the BKZ crossing should sit. At n=163 BKZ lags SD
#    by 1.31x (SD 2.47x vs BKZ 3.23x) => expected 157 BKZ crossing ~2.7-3.0x. These 4 cells
#    extend the 157 ladder 2.29->3.07x to (a) pin the 157 BKZ onset = 4th dim point for the
#    onset-gap trend (89/101/113/157), (b) chart SD's post-onset climb at the last clean-onset
#    dim (saturates high like 149/151 vs wobbles like 167's soft-wall — gradient story).
#    COST (MEASURED, q2203/q2411 cells): ~7.7h/seed flat => ~8h/cell (20 seeds/20 workers,
#    1 wave), ~32h total. 24h DEFAULT_MAX_WALL_H backstop clears. All q prime, canonical
#    results/seeds/ntru/ tree (merges with existing 157 ladder), campaign ntru_n157_qsweep
#    (n157 beta40 mt50 p500 mpfr — dim 314, beta40 short-enum, no INC-54 wedge).
ntru_n157_qsweep --q 2609 --workers 20
ntru_n157_qsweep --q 2903 --workers 20
ntru_n157_qsweep --q 3203 --workers 20
ntru_n157_qsweep --q 3499 --workers 20
#    ^ DONE 2026-08-20: BKZ onset pinned at 2740 (2.40x q_fat), SD 2354 (2.07x), lag 1.16x.
#      paper_findings 2026-08-20.
#
# #4 n=163 BKZ-onset extension (2026-08-21). WHY: 163 is the last clean-onset dim and
#    the steepest onset-gap point (31% vs 16% at 157), but its BKZ crossing q=4049 is the
#    GRID EDGE at exactly 0.50/20 (prior cells 0.20-0.35) -- one seed flips the readout.
#    Same under-sampling #3 fixed at 157. 5 cells 3.37->3.85x q_fat: pin the 163 BKZ onset
#    (5th gap-trend point) + chart SD post-onset (saturates like 149/151 vs stalls ~60%
#    = 167 soft-wall precursor). COST MEASURED (bkz_time+sdbkz_time, q3793/3919/4049
#    cells): 8.3h/seed flat, 20 seeds/20 workers = 1 wave, ~8.3h/cell, ~42h total.
#    24h DEFAULT_MAX_WALL_H backstop clears. beta40 dim 326 short-enum, no INC-54 wedge.
#    --n is a CLI override (163 not in n_grid, same as the 173 lines). Canonical
#    results/seeds/ntru/ tree, merges with the existing 163 ladder (2503-4049).
ntru_onset_boundary --n 163 --q 4211 --workers 20
ntru_onset_boundary --n 163 --q 4357 --workers 20
ntru_onset_boundary --n 163 --q 4507 --workers 20
#    DESKTOP STARVATION (2026-08-22, HOTO 1.8): 20 fplll workers at nice 0 starved
#    gnome-shell/gnome-terminal (load 21.5, RCU stalls). Fix is PRIORITY, not width:
#    systemd drop-in ~/.config/systemd/user/forever-runner.service.d/nice.conf sets
#    Nice=19, which campaign children inherit. Workers deliberately stay at 20 -- a
#    cut to 16 breaks the 1-wave design (16+4 => ~9h -> ~17h/cell) for no added safety.
ntru_onset_boundary --n 163 --q 4657 --workers 20
ntru_onset_boundary --n 163 --q 4813 --workers 20
#    ^ DONE 2026-08-23: all 5 cells landed (results/logs/forever_worklist.done, exit 0,
#      2026-08-21 15:09 -> 2026-08-23 04:56 UTC). READOUT OWED: re-run
#      scripts/extract_dsd_onset.py over the merged 163 ladder (2503-4813) to pin the BKZ
#      crossing and add the 5th onset-gap point, then append to paper_findings.md. Until
#      that is done this line is complete but unreported.
#
# ---- EXAMPLES you can uncomment / add (frontier q-cells, onset-knee topups) ----
# ntru_onset_boundary --n 173 --q 5501 --workers 20      # push 173 past its wall stop
# ntru_onset_boundary --n 191 --q 4513 --workers 20      # extend ladder to a new prime

# ==== 2026-08-28: matched β=50 ladder against the β=40 baselines ==============
#
# WHY. On 2026-08-28 the deck returned n=181 β=50 q=4591 (2.830× q_fat) at 4/9
# integer-exact recoveries, against a β=40 null of 0/120 at the same dimension
# spanning 1.90–3.62× q_fat (paper_findings.md:4625). That is one point. These
# cells turn it into an onset curve, and every q below is a q where the SAME
# dimension at β=40 is already on disk with a known outcome — so each line is a
# PAIRED β-contrast at identical (n, q), not a cross-q comparison.
#   n=181 β=40 baselines: q3083/3407/3733/4871/5381/5869 all 0/20.
#   n=179 β=40 baselines: q3163/3637/4111/5077 — the FROZEN-vs-collapse ladder.
# q=2593 (1.598×) and q=6491 (4.001×) have no β=40 twin; they widen the bracket.
#
# COST, measured not guessed. β=50 at dim ~362 costs 15.1 h/seed (n=181, 9 seeds)
# and 13.2 h/seed (n=179, 4 seeds), from the completed seeds' bkz_time+sdbkz_time.
# estimate_sweep_time.py self-anchors 10 seeds on 6 workers at 30.2 h. With
# --workers 20 and --seeds 20 every seed runs concurrently, so cell wall ≈ one
# seed. 8 × 15.1 h + 4 × 13.2 h = 173.6 h ≈ 7.2 days.
#
# --seed-timeout-s 86400 is deliberate: measured worst case is 15.23 h = 54828 s,
# so 24 h caps a genuinely stuck seed without killing a healthy one. Do NOT drop
# to the sweep.toml default of 14400 — that is 0.30× a healthy seed and kills
# every one by construction (INC-56, and see the wall_cap_events note above).
#
# Ordered to find the onset first: bracket down from the known 2.830× success,
# confirm above it, then widen, then the n=179 ladder.
# NOT deck-owned — the deck owns q4591 β50/β60 at n=179 and n=181 only.
#
# STATUS 2026-08-28: first arming of these 12 lines was a silent no-op. The
# runner dispatched them host-side, where the g6k module does not exist; all
# 240 seeds died in seconds with "ModuleNotFoundError: No module named 'g6k'",
# run_campaign returned rc=0 ("0 seeds written / 20 tasks"), and every line was
# archived as done in ~12 minutes (13:46-13:53 AEST). Zero compute was burned
# and no data was touched. Fixed same day: run_campaign now re-execs g6k
# campaigns inside sdbkz-g6k:dim384 (docker, --user, nice -n 19) and returns
# rc=1 when every attempted seed fails; the 12 done-log entries were removed to
# re-arm. See scripts/run_campaign.py (_g6k_container_argv) and
# tests/test_run_campaign_g6k_wrap.py.
ntru_wall_beta_bump --n 181 --q 3733 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 181 --q 4871 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 181 --q 3407 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 181 --q 3083 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 181 --q 5381 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 181 --q 5869 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 181 --q 2593 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 181 --q 6491 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 179 --q 4111 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 179 --q 3637 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 179 --q 3163 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
ntru_wall_beta_bump --n 179 --q 5077 --beta 50 --seeds 20 --workers 20 --seed-timeout-s 86400
