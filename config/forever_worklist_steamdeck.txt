# Steamdeck node worklist — forever-runner profile (rootless podman, 6 physical
# Zen 4 cores; unit: ~/.config/systemd/user/forever-runner.service on the node,
# repo copy config/forever-runner-steamdeck.service).
#
# HOW TO INJECT WORK (owner): edit this file on the workstation, then
#     make node-push-worklist
# The runner re-reads this file at every loop iteration — no restart needed.
# Completed lines are tracked in results/logs/forever_worklist.done ON THE NODE;
# this file is never mutated by the runner. Editing a completed line's text
# makes it a NEW line (whitespace-normalized signature).
#
# LINE FORMAT (same as config/forever_worklist.txt):
#     <campaign> [--flags...] [@budget_h=H]
#   - ALWAYS pass --workers 6 or less (run_campaign defaults to 22; the node
#     has 6 cores; g6k sieve stays threads=1, parallelism is across seeds).
#   - Long seeds: pass --seed-timeout-s per line (INC-56 per-seed SIGKILL cap)
#     AND @budget_h=H for the whole line; without @budget_h the runner's
#     24 h backstop kills the line (DEFAULT_MAX_WALL_H, INC-54).
#
# CELL OWNERSHIP (collision rule): this node writes ONLY the ntru_g6k tree.
# Deck-owned cells: the q4591 β50 wall cells (n=179, n=181) and the g6k β40
# backfill twins 167:3167 / 173:4073 / 179:4591. The workstation must not
# generate seeds in those cells — node-pull (rsync --ignore-existing) would
# meet two different seeds claiming one filename and silently keep only the
# workstation's. Seeds flow back via `make node-pull` only.
#
# Idle-filler floor (runs whenever this list is drained): ntru_g6k_backfill
# β40, least-sampled-first over the three cells above, ceiling ratchet — the
# node never idles and never exhausts.
#
# ---- REAL SCIENCE (drains top-to-bottom before any filler) -------------------
ntru_wall_beta_bump --n 181 --q 4591 --beta 50 --seeds 10 --workers 6 --seed-timeout-s 86400 @budget_h=48
# ---- 2026-08-28 additions: β=60 bootstrap, then the deck-owned q4591 top-ups --
#
# β=60 IS UNMEASURED EVERYWHERE. estimate_sweep_time.py has no per-tour cost rows
# at β=60 and refuses to predict: "Run a single seed at the target configuration
# to bootstrap." So this is a 2-seed BOOTSTRAP, not a campaign. Do not queue a
# full β=60 block until its real per-seed cost is read off these two. β=50 costs
# 15.1 h/seed at dim 362; β=60 could be several times that, and guessing is
# exactly the error that put the n=181 estimate 5× low.
# Cap 172800 s = 48 h/seed: generous enough not to kill a healthy β=60 seed,
# finite enough that a wedge cannot eat the week.
# HELD 2026-09-01: same INC-63 hold as the n=179 line below — β=60 aborts the g6k
# gauss_sieve assert (sieving.cpp:118) on the current image. This bootstrap already
# ran on the deck 2026-08-28 (rc=1, both seeds aborted, done-log blocks a rerun);
# commented for consistency so a done-log reset cannot resurrect it before the fix.
# MEASURED 2026-09-02: seed 1 of this bootstrap ran on the WORKSTATION on
# sdbkz-g6k:dim384-inc63 (container runcamp-g6k-inc63-validate): 38.5 h/seed
# (bkz 13.6 h + sdbkz 24.9 h, 50 tours each), rc=0, both legs integer-exact,
# 5 tolerant swaps, 0 asserts (incidents.md INC-63 addendum 7). Under the 40 h/seed
# rule below by 1.5 h. Seed 2 is unrun. Stays HELD until the inc63 image ships to
# the deck (podman load); the workstation seed 1 JSON is not on the deck, so
# un-holding --seeds 2 here would rerun seed 1 (38.5 h) before seed 2.
# IMAGE SHIPPED 2026-09-02 20:30 AEST: sdbkz-g6k:dim384-inc63 loaded on the deck
# (podman), unit ExecStart repointed (config/forever-runner-steamdeck.service),
# one-shot watcher inc63-cutover.service restarts the runner once the current
# beta=40 filler cell has 20/20 seeds on disk (fired 03:49:58 AEST 2026-09-03). After that
# every line here runs on the tolerant image. Un-holding any beta=60 line is the
# owner's call (INC-63 addendum 8): the science line below is the one to weigh.
# ntru_wall_beta_bump --n 181 --q 4591 --beta 60 --seeds 2 --workers 6 --seed-timeout-s 172800 @budget_h=48

# STATUS 2026-09-02: this line is DONE on the deck — 20/20 seeds pulled by
# 2026-09-01 (union 14/20 integer-exact: SD 11, BKZ 6, three both-leg). The note
# below is the pre-run record.
# n=179 β=50 top-up to 20. Currently 4 seeds (1/4 SD-crack, 2026-08-19); seeds
# 5-20 were all wall-cap killed at 14400 s = 0.30× a healthy seed, so that cell
# carries NO rate. This is the replication the 2026-08-20 entry asked for, and it
# is the cell that decides whether the SD advantage is real or whether n=181's
# 2/9-vs-2/9 parity is the true picture. Existing seeds resume-skip.
ntru_wall_beta_bump --n 179 --q 4591 --beta 50 --seeds 20 --workers 6 --seed-timeout-s 86400 @budget_h=48

# STATUS 2026-09-02 08:50: this line is DONE on the deck (rc=0, 2026-09-02 00:20 AEST);
# 19/20 pulled (union 10/19: SD 7, BKZ 4). seed0002 aborted on BOTH attempts
# (2026-08-25 workstation, 2026-08-31 deck; INC-63, per-seed deterministic under
# threads=1) and is rerun on the workstation's dim384-inc63 line, not here. The note
# below is the pre-run record.
# n=181 β=50 top-up to 20. Currently 9 (4/9). seed0002 is absent and is NOT in
# wall_cap_events.jsonl, so it was not cap-killed — this re-runs it and doubles
# the sample on the cell the whole finding rests on.
ntru_wall_beta_bump --n 181 --q 4591 --beta 50 --seeds 20 --workers 6 --seed-timeout-s 86400 @budget_h=40

# 2026-09-02 (owner: "queue one"): g6k short-vector probe. Reruns seeds 1..8 of
# the n=179 q=4591 beta=50 cell with store_short_vectors = true so the sub-q^2
# non-secret legs (norm^2 10-20k) can be tested offline for dense-sublattice
# membership -- design and readout in config/sweep.toml [campaigns.ntru_g6k_vecprobe].
# Own tree results/seeds/ntru_g6k_vecprobe/ (arrives via node-pull), never ntru_g6k/.
# Unpatched dim384 is fine here: that cell landed 20/20 with 0 INC-63 aborts and the
# abort is per-seed deterministic. 8 seeds at 6 workers = 2 waves, ~13.4 h/seed ->
# ~27 h; @budget_h=40. Picked up at the next line boundary after the current
# beta=40 filler cell (started 13:26 AEST 2026-09-02, ~21 h).
ntru_g6k_vecprobe --n 179 --q 4591 --beta 50 --seeds 8 --workers 6 --seed-timeout-s 86400 @budget_h=40

# PROVISIONAL — only meaningful once the bootstrap above has reported a cost.
# If β=60 turns out to cost more than ~40 h/seed, delete this line rather than
# let it run: at 6 workers it would spend most of a week on one cell.
# COST READ 2026-09-02: 38.5 h/seed at n=181 (one seed, patched image), so the
# >40 h delete rule does NOT fire and this line survives; n=179 should run a
# little cheaper (beta=50: 13.2 vs 15.1 h/seed). 10 seeds at 6 workers = two
# waves ~77 h, inside @budget_h=96. Still HELD: the deck image is dim384
# (unpatched) and beta=60 asserts deterministically on it.
# 2026-09-02 20:30: the image is now shipped and the cutover is armed (see the
# bootstrap note above) -- the only remaining hold reason is the owner's go.
# 2026-09-03 03:49:58 AEST: cutover FIRED -- runner restarted on dim384-inc63 at the
# n=179 beta=40 q4591 20/20 boundary, vecprobe line started 03:50:58. Owner's go remains.
# Cost for this line on inc63: 10 seeds x ~35-38 h at 6 workers = two waves,
# ~75 h, inside @budget_h=96. It would run after the vecprobe line above.
# HELD 2026-08-31: beta=60 crashes the g6k gauss_sieve assert (sieving.cpp:118) on the current image — 9/9 aborts, 0 seeds ever written. Do not re-enable until the kernel fix ships (see session notes / incident pending).
# ntru_wall_beta_bump --n 179 --q 4591 --beta 60 --seeds 10 --workers 6 --seed-timeout-s 172800 @budget_h=96
ntru_g6k_vecprobe --n 179 --q 3163 --beta 50 --seeds 8 --workers 6 --seed-timeout-s 86400 @budget_h=40
ntru_g6k_vecprobe --n 179 --q 4591 --beta 40 --seeds 8 --workers 6 --seed-timeout-s 86400 @budget_h=40
